<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}){faint} %clr(%5p) %clr([%X{traceId:-no-trace}]){cyan} %clr(%-40.40logger{39}){blue} %clr(:){faint} %m%n%wEx"/>

    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="dev.unzor.nexus" level="INFO"/>
    <logger name="org.springframework.modulith" level="INFO"/>
</configuration>
